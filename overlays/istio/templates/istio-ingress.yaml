apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: ingress
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "{{ .Values.istio.ingress.sync_wave }}"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  destination:
    name: {{ .Values.global.destination.name }}
    namespace: istio-ingress
  project: {{ .Values.global.project }}
  sources:
  - repoURL: https://istio-release.storage.googleapis.com/charts
    chart: gateway
    targetRevision: 1.18.0
    helm:
      valueFiles:
        - $values/{{ .Values.global.source.path }}/files/istio-ingress-values.yaml
  - repoURL: {{ .Values.global.source.repoURL }}
    targetRevision: {{ .Values.global.source.version }}
    ref: values
  syncPolicy:
    syncOptions:
    - CreateNamespace=false
    automated:
      prune: {{ .Values.global.syncPolicy.automated.prune }}
      selfHeal: {{ .Values.global.syncPolicy.automated.selfHeal }}
